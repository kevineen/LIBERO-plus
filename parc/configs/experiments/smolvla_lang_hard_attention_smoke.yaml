# 注視マップ配線スモーク（短尺 · 1 task × 1）
name: smolvla_lang_hard_attention_smoke
seed: 0
tags: [eval, smolvla, language, hard, attention, smoke]

eval:
  suite: libero_spatial
  num_trials_per_task: 1
  task_ids: [984]
  max_steps: 40
  camera_height: 256
  camera_width: 256
  save_video: true
  save_frames: false
  frame_stride: 4
  attention_stride: 4
  max_save_frames: 20
  save_attention: true
  attention_on_failure_only: true
  attention_method: activation
  use_classification: true

policy:
  type: checkpoint
  action_dim: 7
  device: cuda
  flip_images: true
  path: experiments/20260730T184742Z_winpc_c7e7c8f2_smolvla_ft_libero_cam_mix_lr5e6_5k_winpc/train_output/checkpoints/005000/pretrained_model
