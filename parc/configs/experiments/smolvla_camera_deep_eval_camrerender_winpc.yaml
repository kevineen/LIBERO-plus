# winpc: camera rerender FT 後の Camera deep eval（608–612 ×10）
name: smolvla_camera_deep_eval_camrerender_winpc
seed: 0
tags: [eval, smolvla, camera, deep, camrerender, winpc]

eval:
  suite: libero_spatial
  num_trials_per_task: 10
  task_ids: [608, 609, 610, 611, 612]
  max_steps: 280
  camera_height: 256
  camera_width: 256
  save_video: true
  save_frames: false
  use_classification: true

policy:
  type: checkpoint
  action_dim: 7
  device: cuda
  flip_images: true
  # FT 完了後に checkpoints/010000/pretrained_model へ差し替え
  path: /home/kevin/Matsuo/robot/LIBERO-plus/parc/experiments/20260728T010727Z_winpc_cb240365_smolvla_ft_continue30k_finetune15k_winpc/train_output/checkpoints/015000/pretrained_model
