# thor: winpc +15k 親候補を厚い eval（親候補のクロス確認）
# winpc 厚いは 0.371。同一 ckpt を thor で再評価してマシン差を分離する。学習なし。
name: smolvla_thick_eval_winpc15k_on_thor
seed: 0
tags: [eval, smolvla, thick, finetune15k, cross_machine, parent_pick, thor]

eval:
  suite: libero_spatial
  num_trials_per_task: 1
  task_ids: null
  tasks_per_category: 5
  max_steps: 280
  camera_height: 256
  camera_width: 256
  save_video: true
  save_frames: false
  use_classification: true

policy:
  type: checkpoint
  action_dim: 7
  device: cuda
  flip_images: true
  path: /mnt/sda/parc_libero_plus/experiments/imported/20260728T010727Z_winpc_cb240365_smolvla_ft_continue30k_finetune15k_winpc/015000/pretrained_model
